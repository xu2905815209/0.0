# 奥凯杯智能小车项目背景文档

> 本文档专为 AI 助手设计，快速了解项目背景、硬件架构、控制策略。

## 1. 项目概述

- **赛事**：奥凯杯智能小车竞赛
- **目标**：麦克纳姆轮小车在走廊环境中自主循迹导航
- **核心任务**：小车沿走廊中线前进，保持正姿态（不斜着走）
- **当前版本**：v0.3（控制逻辑重构版）

## 2. 硬件架构

### 2.1 主控平台
- MCU: STM32F407（HAL库开发）
- 开发环境: Keil MDK-ARM / IAR EWARM
- CubeMX配置文件: `test2.ioc`

### 2.2 麦克纳姆轮底盘
- **4个麦克纳姆轮**：A/B/C/D（支持全向移动）
- **运动能力**：
  - `vx`: 前进/后退
  - `vy`: 横向平移（左/右）
  - `wz`: 原地旋转/姿态调整
- **轮序映射**：
  - wheel0 = A (TIM5编码器)
  - wheel1 = B (TIM3编码器)
  - wheel2 = C (TIM4编码器)
  - wheel3 = D (TIM1编码器)

### 2.3 超声波传感器布局

小车朝向走廊前方时的传感器位置：

```
    前方（小车前进方向）
        ↑
   D5(左前)    D4(右前)
      |          |
      |   小车   |
      |          |
   D1(左后)    D2(右后)
        ↓
    后方
```

| 通道 | 位置 | 型号 | 原始数据格式 |
|------|------|------|--------------|
| D1 | 左后 | HCSR04 | 回波宽度 μs |
| D2 | 右后 | HCSR04 | 回波宽度 μs |
| D3 | 前向 | HCSR04 | 回波宽度 μs（暂未用于闭环） |
| D4 | 右前 | US016 | ADC值（0~4095） |
| D5 | 左前 | US016 | ADC值（0~4095） |
| D6 | 前向远距 | KS103 | I2C距离cm |

### 2.4 其他传感器
- **IMU**: JY61P陀螺仪（UART，提供 yaw 角度）
- **编码器**: 4路正交编码器（TIM1/3/4/5）

### 2.5 通信接口
- **串口1**: 蓝牙/调试命令输入 + 遥测输出
- **串口2/3/4**: 传感器通信（超声波、IMU等）
- **I2C**: KS103超声波通信

## 3. 控制策略

### 3.1 核心控制理念（v0.3重构后）

**问题**：旧版本只有单一偏差值，小车虽在中线但姿态可能斜着。

**解决方案**：前后两对超声波独立闭环 + 姿态控制。

```
控制框图：

前对闭环:  D5(左前) - D4(右前) → 前对误差 → PID → vy_front
后对闭环:  D1(左后) - D2(右后) → 后对误差 → PID → vy_rear

姿态闭环:  前对误差 - 后对误差 → 姿态误差 → PID → wz

综合输出:
  vx = 前进速度（给定）
  vy = (vy_front + vy_rear) / 2  → 整体横移
  wz = 姿态PID输出 → 姿态修正角速度

麦轮逆解:  body_to_wheels(vx, vy, wz) → 4轮目标速度
速度内环:  每轮独立PID → PWM输出
```

### 3.2 控制模式

| 模式 | 说明 |
|------|------|
| IDLE | 空闲，电机停止 |
| MANUAL_VEL | 手动速度控制（vx, vy, wz给定） |
| LINE_FOLLOW | 走廊循迹模式（双闭环+姿态控制） |
| CALIBRATION | 超声波校准模式 |

### 3.3 控制周期
- **主控任务**: 10ms（TIM6中断触发 `Control_10ms_Task()`）
- **遥测输出**: 100ms周期（主循环）

## 4. 关键代码结构

```
Core/
├── Inc/
│   ├── control.h      # 控制模块接口定义
│   ├── supvc.h        # 超声波驱动接口
│   ├── pid.h          # PID算法实现
│   ├── motor.h        # 电机驱动接口
│   └── jy61p_uart.h   # IMU接口
├── Src/
│   ├── control.c      # 控制主逻辑（核心修改文件）
│   ├── supvc.c        # 超声波驱动实现
│   ├── main.c         # 主函数入口
│   ├── usart.c        # 串口通信 + 命令解析
│   └── i2c.c          # I2C通信（KS103）
```

## 5. 命令协议

### 5.1 单字符命令（蓝牙推荐）

| 按键 | 功能 |
|------|------|
| 1 | 开启走廊循迹模式 |
| 2 | 停止循迹 |
| 3/4 | 开启/关闭遥测 |
| 6/7 | line_target ±5 |
| 8/9 | 前进速度 ±20 mm/s |
| 0 | 复位控制状态 |

### 5.2 CSV命令协议

```
CMD,MODE,<IDLE|MANUAL|LINE|CAL>
CMD,VEL,<vx>,<vy>,<wz>
CMD,LINE,START,<vx>,<target>
CMD,LINE,TARGET,<target>
CMD,LINE,STOP
CMD,YAW,ON|OFF|TARGET,<deg>
CMD,PID,LINE,<kp>,<ki>,<kd>
CMD,PID,WHEEL,<kp>,<ki>,<kd>
CMD,PARAM,GET
```

## 6. 调试与遥测

### 6.1 FireWater遥测格式（已实现）
周期输出控制状态快照，用于离线分析。

### 6.2 超声波原始数据输出
```
US6,D1:x.x,D2:x.x,D3:x.x,D4:x.x,D5:x.x,D6:x.x
```
单位：cm，100ms周期输出。

## 7. 现场调参建议

### 7.1 超声波归一化校准
1. 进入CALIBRATION模式
2. 小车在走廊中移动到不同位置
3. 记录min/max原始值
4. 使用 `CMD,CAL,SET,<ch>,<min>,<max>` 更新

### 7.2 PID参数调整顺序
1. 先调 **速度内环**（WHEEL PID）
2. 再调 **位置外环**（LINE PID）
3. 最后调 **姿态环**（姿态PID）

## 8. 常见问题

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 小车斜着走 | 只用单一偏差 | 启用双闭环+姿态控制 |
| 横移方向反 | 麦轮逆解符号 | 检查 `body_to_wheels()` |
| 超声波跳变 | 噪声干扰 | 增大IIR滤波alpha |
| 姿态漂移 | IMU零偏 | 启用yaw_hold |

## 9. Git协作约定

- 主分支: `feature/0.1`
- 稳定基线: `v0.0.0`
- 提交规范: `feat:`/`fix:`/`refactor:`/`docs:`
- 详细协作说明见 `README.md`

---

**文档维护**：每次重大修改后更新此文档。
**最后更新**：2026-04-07