# Serial Center Analysis

Generated capture sessions are stored under `analysis/serial_center/sessions/`.

Each session directory contains:

- `raw.log`
- `frames.csv`
- `summary.md`
- plot images

These artifacts are intended to be reused as the baseline for later firmware tuning.

